services:
  ttlock-helper:
    build: .
    container_name: ttlock-helper
    ports:
      - "8005:8000"
    environment:
      - TZ=Australia/Sydney
      - CONFIG_PATH=/data/config.json
      - LOG_PATH=/data/app.log
    volumes:
      - ./data:/data
    restart: unless-stopped
